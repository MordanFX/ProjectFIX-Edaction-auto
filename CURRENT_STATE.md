# CURRENT_STATE.md

Текущее состояние проекта на 2026-07-16.

Этот файл нужен, чтобы не восстанавливать контекст из переписки перед каждой задачей.

## Главная продуктовая схема

Есть два отдельных потока:

1. Telegram bot + web panel.
2. Discord homework + web panel.

Оба потока попадают в веб-панель, но ученики, курсы и рабочие сценарии должны оставаться разделёнными.

## Telegram-поток

### Уже работает

- `/start` регистрирует Telegram-пользователя.
- Telegram-ученик может получить доступ к курсу.
- Уроки идут поэтапно.
- Урок может содержать несколько материалов/видео.
- Для уроков без ДЗ показывается, что сдавать ничего не нужно.
- Для уроков с ДЗ ученик может сдать работу.
- Ученик может отправлять текст, файл, фото или видео.
- Куратор может проверять Telegram-ДЗ из Telegram-режима или из веб-панели.
- Куратор может принять работу или отправить на доработку.
- Ответ куратора может содержать текст и вложение.
- Ученик получает результат проверки обратно в Telegram.
- Telegram-курсы и ученики отображаются отдельно от Discord.

### Важно

- Telegram-бот и веб-панель не вызывают друг друга напрямую.
- Бот и панель работают через базу.
- У куратора в Telegram должен быть `telegram_user_id`, привязанный к `staff_users`.

### Что ещё требует улучшения

- Удобная страница управления командой/кураторами.
- Скрывать кураторов из списка Telegram-учеников, если они нажали `/start` только для режима куратора.
- Более понятная выдача курса через Telegram-режим куратора.
- История действий куратора.
- Закрепление работы за конкретным куратором.

## Discord-поток

### Уже работает

- Discord-бот подключён к серверу клиента.
- Онбординг нового ученика: панель выдаёт invite-ссылку + персональный одноразовый код
  (HMAC, показывается один раз). Ученик заходит в read-only канал `#start-for-homework`,
  жмёт кнопку «Получить доступ», вводит код в модалку — бот создаёт приватную ветку,
  впускает лично его и назначает курс из инвайта. Проверено вживую 2026-07-16.
- Slash-команда `/homework` осталась как запасной вход (для staff/тестов).
- У каждого Discord-ученика своя приватная ветка/пространство.
- Ученики не видят ветки друг друга.
- Код при неудаче создания ветки возвращается (не сгорает), можно повторить.
- Права: у бота ТОЛЬКО канальные права (без Manage Server / ролей). На `homework-test`
  боту нужны все 8: Просмотр, Отправлять, Читать историю, Прикреплять файлы,
  Управлять правами, Управлять ветками, Создавать приватные ветки, Писать в ветках.
  Бот при работе перезаписывает свой overwrite на канале — набор в
  `homework.py` должен содержать все эти права (урок от 2026-07-16, самозатирание).
- Discord-ученик может отправить ДЗ на проверку.
- У Discord-сообщения с ДЗ есть кнопка отправки на проверку.
- Есть сценарий вопроса к куратору.
- Вопросы попадают в панель/очередь вопросов.
- Discord-ДЗ попадают в веб-панель.
- Куратор может принять работу или отправить на доработку из веб-панели.
- Ответ куратора может содержать текст и вложение.
- Ответ уходит обратно в приватную Discord-ветку.
- В панели есть история попыток по ДЗ.
- В панели можно открыть исходное сообщение в Discord.
- Фото/изображения ученика можно просматривать из панели.

### Discord-права

Доступ к приватным Discord-веткам задаётся не веб-аккаунтом, а ролями Discord.

Роли, которые обсуждались как staff:

```text
Founder
Main Trader's Department
Support Department
```

Их ID, которые передавались для staff mention:

```text
1324489727155372174
1400407463584338001
1437783918802894951
```

Для доступа к private homework-веткам в Discord нужно настроить права канала/категории:

```text
Просмотр канала
Отправлять сообщения
Читать историю сообщений
Отправлять сообщения в ветках
Просматривать приватные ветки
```

Если куратор не видит ветку, проблема обычно в Discord permissions/roles, а не в веб-панели.

## Веб-панель

### Уже есть разделы

- Telegram ДЗ.
- Telegram ученики.
- Discord.
- Выдать ДЗ Discord.
- Discord ученики.
- Доступы.
- Курсы.
- База знаний.

### Работает

- Логин в панель через `staff_users`.
- JWT-авторизация.
- Mordan имеет аккаунт владельца/admin.
- Проверка Telegram и Discord ДЗ.
- Просмотр вложений ученика.
- Отправка feedback с вложением.
- Курсы Telegram и Discord разделены.
- База знаний показывает уроки из Telegram/Discord-курсов.
- Discord-урок можно выдать ученикам в приватные ветки.

### Нужно улучшать

- Страница `Команда`.
- Создание отдельных аккаунтов кураторов.
- Привязка Telegram ID к куратору.
- Статистика куратора.
- Статистика команды.
- Механика “взять работу в проверку”.
- Скрытие staff из Telegram-учеников.
- Уведомления/счётчики по новым работам и вопросам.

## Кураторы/люди

### Владелец

```text
Mordan
role: admin
```

### Кураторы, которых нужно завести отдельными аккаунтами

По последнему обсуждению минимум:

```text
Doc.              curator
Влад Стрельников curator
Артём             curator
```

Эти люди тестировали Telegram-режим куратора. Их нужно не оставлять просто Telegram-учениками, а привязать к `staff_users`.

## Серверное состояние

Текущий проект развернут на Hetzner:

```text
host: 167.233.113.236
path: /opt/projectfix-education
```

Публичная панель:

```text
http://167.233.113.236:8081
```

Последний известный deployed commit:

```text
7a67979 Send Vimeo links through the player and report completed lessons honestly
```

Важно про Vimeo (2026-07-18): все учебные видео в Vimeo-аккаунте «Project» стоят в
режиме «Hide from Vimeo» — страница `vimeo.com/ID` отдаёт 404 всем, кроме владельца,
работает только встраивание. Поэтому бот и Discord-доставка переписывают ссылки в
`player.vimeo.com/video/ID` (`vimeo_watch_url` в `integrations/vimeo.py`). Если владелец
переключит видео на «Unlisted», ссылки в базе нужно заменить на полные с хэшем —
`vimeo_watch_url` пронесёт хэш как `?h=`.

Деплой-нюанс: после пересоздания контейнера `api` nginx во `frontend` держит старый
IP upstream и панель отдаёт 502 — нужен `docker compose restart frontend`.

Прод-`.env` на сервере: `DISCORD_INVITE_CHANNEL_ID=1527072325479174184`
(`#start-for-homework`, вход с кнопкой), `DISCORD_HOMEWORK_CHANNEL_ID=1526145379987623956`
(`homework-test`, скрытый «стол» с приватными ветками).

Ранее начатые изменения по странице команды остановлены и не должны считаться
завершёнными, пока не будут сделаны заново и проверены.

## Последнее завершённое крупное изменение

Discord-онбординг по персональным кодам доступа (2026-07-16):

- Миграция `20260716_01`: `access_code_digest` / `used_at` / `used_by_discord_user_id`.
- Панель: «Создать доступ» → ссылка + одноразовый код, статусы инвайтов
  (ждёт активации / активирован / истёк).
- Бот: кнопка «Получить доступ» → модалка кода → приватная ветка; `/setup_welcome`
  публикует приветствие с кнопкой в канал входа.
- Протестировано на реальном новом ученике: ветка создана, изоляция подтверждена,
  ученик появился в панели.
- Проверки перед деплоем проходили:

```text
ruff check .
pytest --cov
npm run build
```

## Текущая продуктовая цель

Следующий большой блок — команда кураторов:

1. Отдельные аккаунты кураторов.
2. Привязка Telegram ID.
3. Раздел `Команда`.
4. Закрепление ДЗ за куратором.
5. Статистика личная и командная.

Делать поэтапно, не одним большим изменением.
